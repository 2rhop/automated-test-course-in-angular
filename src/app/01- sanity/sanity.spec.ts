describe('Comprobar jasmine + karma:', () => {
    it('debe devolver true', () => {
        expect(true).toBeTruthy();
    });
    it('debe sumar 1 + 1', () => {
        expect(1 + 1).toBe(2); 
    });
});